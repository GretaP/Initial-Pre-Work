// Recap: For some reason, I tend to use commas rather than semicolons in for loops and not notice until I try to run the program.  :)

// Example
var ourArray = [];

for (var i = 0; i < 5; i++) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.
for (i=0; i<5; i++){
    myArray.push(i+1);
}
